{
  "_comment": "docs/02 §2.3 in JSON. status: proposed | running | halted | ended. composition: pareto | oec (oec requires oec_weights: {metric_id: weight}). mode: optimize | instrument (instrument inverts frozen and protected roles per docs/07 §7.3). walls are the five walls of docs/04 plus the pre-registration and anomaly-breaker checks; every one defaults to true and turning one off is recorded in the ledger and the report. Placeholders in {{...}} are filled at gate 1; the harness rejects a campaign that still contains one.",
  "schema_version": 1,
  "id": "{{campaign_id}}",
  "created_at": "{{iso_timestamp}}",
  "mode": "optimize",
  "goals": ["{{goal_metric_id}}"],
  "guardrails": ["{{guardrail_metric_id}}"],
  "diagnostics": [],
  "inherited_ratchet": [],
  "composition": "pareto",
  "oec_weights": null,
  "budget": {"dollars": 40, "hours": 8, "experiments": 60},
  "plateau_patience": 8,
  "protected_paths": ["{{protected_path}}"],
  "frozen_paths": ["{{frozen_path}}"],
  "eval_hash": null,
  "branch": "sb/{{campaign_id}}",
  "base_commit": "{{head_commit}}",
  "status": "proposed",
  "halt_reason": null,
  "walls": {
    "validity": true,
    "noise_floor": true,
    "confirm": true,
    "holdout": true,
    "frozen_guard": true,
    "judge": true,
    "prereg": true,
    "anomaly_breaker": true
  },
  "iteration_cap": 200,
  "max_parallel": 2,
  "distill_every": 8,
  "false_promotion_budget": {"window": 10, "max_fraction": 0.4},
  "holdout_rotation_every": 10,
  "side_branch": {"budget_fraction": 0.15, "max_stones": 3},
  "auto_pr": false,
  "gate1": {"approved_by": null, "approved_at": null, "edited_fields": []}
}
