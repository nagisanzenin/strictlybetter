---
description: "strictlybetter — distill the ledger now: engine stats, spawn the distiller, store the inheritance body."
argument-hint: ""
---

Load strictlybetter's `distill` skill: find the skill named `distill` in the skills list of your system prompt, read its SKILL.md with the read tool, and follow it exactly as your operating instructions from here on. Run its engine-resolution block VERBATIM first (the strictlybetter extension exports `SB_ROOT` for it) and quote the engine's output; never compute a statistic yourself.

The user's request (empty means: distill now): $@
