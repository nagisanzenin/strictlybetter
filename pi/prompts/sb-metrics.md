---
description: "strictlybetter — discover and instrument metrics: spawn the metrologist, add the cards, measure sigma, probe monotonicity."
argument-hint: "[--repeats N]"
---

Load strictlybetter's `metrics` skill: find the skill named `metrics` in the skills list of your system prompt, read its SKILL.md with the read tool, and follow it exactly as your operating instructions from here on. Run its engine-resolution block VERBATIM first (the strictlybetter extension exports `SB_ROOT` for it) and quote the engine's output; never compute a statistic yourself.

The user's request (empty means: the default repeats): $@
