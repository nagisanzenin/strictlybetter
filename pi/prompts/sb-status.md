---
description: "strictlybetter — campaign status, the cold-start brief, and budget counters, quoted from the engine."
argument-hint: ""
---

Load strictlybetter's `status` skill: find the skill named `status` in the skills list of your system prompt, read its SKILL.md with the read tool, and follow it exactly as your operating instructions from here on. Run its engine-resolution block VERBATIM first (the strictlybetter extension exports `SB_ROOT` for it) and quote the engine's output; never compute a statistic yourself.

The user's request (empty means: the standard status): $@
