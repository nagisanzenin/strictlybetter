---
description: "strictlybetter — universal research loop: orient on this repo, discover metrics, run pre-registered experiments, keep only strictly-better changes."
argument-hint: "[what to improve, in your words] | continue"
---

Load strictlybetter's `strictlybetter` skill: find the skill named `strictlybetter` in the skills list of your system prompt, read its SKILL.md with the read tool, and follow it exactly as your operating instructions from here on. Run its engine-resolution block VERBATIM first (the strictlybetter extension exports `SB_ROOT` for it) and quote the engine's output; never compute a statistic yourself.

The user's request (empty means: continue where the engine says we are): $@
