# textstats

A single-file Python module that reports line, word and character counts, the most common word, and a Flesch reading-ease score for a text file:

    python3 textstats.py some.txt

There are no tests yet, and no benchmark. This is a greenfield fixture for the strictlybetter research loop: the first campaign on it has to build the instruments before anything can be optimised.
